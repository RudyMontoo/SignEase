# SignEase MVP Dependencies
# Core ML and Computer Vision
# Install PyTorch with CUDA support using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.7.0
torchvision>=0.22.0
torchaudio>=2.7.0
opencv-python>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0

# Web Framework
Flask>=3.0.0
flask-cors>=4.0.0

# MediaPipe - Currently not available for Python 3.13
# mediapipe>=0.10.0
# Note: MediaPipe requires Python 3.8-3.12. For Python 3.13, consider:
# 1. Using Python 3.11 or 3.12 in a virtual environment
# 2. Using alternative hand tracking libraries
# 3. Waiting for MediaPipe to support Python 3.13

# Additional utilities (if needed)
Pillow>=10.0.0
requests>=2.31.0